---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
import StoryFeatureCard from '../components/StoryFeatureCard.astro';
import PenMarquee from '../components/PenMarquee.astro';
import TechnicalSpecs from '../components/TechnicalSpecs.astro';
import { Image } from 'astro:assets';
import heroPoster from '../assets/images/artools-pen-studio.png';
import closingFrame from '../assets/images/artools-closing-frame.jpg';
const hero = {eyebrow:"Artools Precision Pen",titleLine1:"A CANETA",titleLine2:"MAIS AVANÇADA",titleLine3:"DO MUNDO.",description:"Uma nova forma de escrever, criar e registrar ideias — com a precisão que acompanha o ritmo do seu pensamento.",primaryButton:"Conhecer a caneta",secondaryButton:"Ver em movimento"};
---
<Layout title="ARTOOLS | Caneta de Precisão e Design Minimalista" description="Conheça a ARTOOLS, uma caneta de precisão apresentada em uma experiência digital responsiva, minimalista e interativa.">
<style is:global>
:root{--paper:#f4f4ee;--ink:#090909;--muted:#75756f;--line:#d4d4cc;--signal:#dfff00;--white:#fff;--ease:cubic-bezier(.16,1,.3,1)}
*{box-sizing:border-box} html{background:var(--paper)} body{margin:0;color:var(--ink);background:var(--paper);font-family:Inter,Arial,sans-serif;overflow-x:hidden}.font-mono,.token{font-family:"JetBrains Mono",monospace}.skip{position:fixed;left:14px;top:-48px;z-index:100;padding:10px 13px;background:var(--signal);color:var(--ink);font:600 11px "JetBrains Mono",monospace;text-decoration:none}.skip:focus{top:14px}
.experience{height:300vh;position:relative}.stage{position:sticky;top:0;height:100svh;min-height:580px;overflow:hidden;background:var(--paper);isolation:isolate}
.video-wrap{position:absolute;inset:0;z-index:-1;overflow:hidden;background:#deded6}.video-wrap:before{content:"";position:absolute;inset:0;z-index:2;pointer-events:none;background:linear-gradient(90deg,rgba(244,244,238,.88) 0%,rgba(244,244,238,.57) 17%,rgba(244,244,238,.08) 41%,transparent 62%),linear-gradient(0deg,rgba(244,244,238,.2),transparent 28%,rgba(244,244,238,.12))}.product-video{width:100%;height:100%;object-fit:cover;object-position:center;display:block;filter:contrast(1.03) saturate(.9);will-change:transform}
.content{position:absolute;z-index:3;left:clamp(20px,6.5vw,104px);top:clamp(108px,13vh,140px);width:min(820px,62vw);opacity:1;transform:none}.eyebrow,.title-line,.description,.actions{will-change:transform,opacity}.eyebrow{display:flex;align-items:center;gap:10px;font:500 10px "JetBrains Mono",monospace;letter-spacing:.19em;text-transform:uppercase;color:#55554f;margin-bottom:20px}.eyebrow:before{content:"";width:8px;height:8px;background:var(--ink);border-radius:50%}.title{margin:0;font-family:"Inter Tight",Inter,Arial,sans-serif;font-size:clamp(64px,8vw,128px);line-height:.81;letter-spacing:-.072em;font-weight:700;text-wrap:balance}.title-line{display:block;overflow:hidden;padding:.16em .05em .08em 0;margin-top:-.16em}.title-line span{display:block}.outline{color:rgba(244,244,238,.1);-webkit-text-stroke:1.3px var(--ink);text-shadow:0 0 18px rgba(255,255,255,.5);font-weight:600}.description{font-size:clamp(14px,1.15vw,18px);line-height:1.55;max-width:500px;margin:21px 0 24px;color:#55554f;letter-spacing:-.015em}.actions{display:flex;gap:12px;align-items:center;flex-wrap:wrap}.button{min-height:52px;padding:0 24px;border:1px solid rgba(9,9,9,.18);border-radius:999px;display:inline-flex;align-items:center;justify-content:center;gap:11px;cursor:pointer;font:600 10px "JetBrains Mono",monospace;text-decoration:none;letter-spacing:.08em;text-transform:uppercase;transition:color .35s var(--ease),border .35s var(--ease),transform .35s var(--ease),box-shadow .35s var(--ease);position:relative;overflow:hidden;isolation:isolate}.button:before{content:"";position:absolute;inset:0;background:#d9d9d3;border-radius:inherit;transform:translateY(105%);transition:transform .45s var(--ease);z-index:-1}.button:hover,.button:focus-visible{color:var(--ink);border-color:rgba(9,9,9,.34);transform:translateY(-3px);box-shadow:0 12px 30px rgba(9,9,9,.12)}.button:hover:before,.button:focus-visible:before{transform:none}.button.primary{background:var(--ink);border-color:var(--ink);color:white;min-width:186px}.button.primary:hover{color:var(--ink)}.button.secondary{background:rgba(255,255,255,.78);backdrop-filter:blur(10px);min-width:158px}.button.secondary svg{transition:transform .35s var(--ease)}.button.secondary:hover svg{transform:translateX(4px)}button:focus-visible,a:focus-visible{outline:3px solid var(--signal);outline-offset:3px}
.scroll-cue{position:absolute;left:clamp(20px,4vw,68px);bottom:33px;z-index:4;display:flex;gap:10px;align-items:center;font:500 9px "JetBrains Mono",monospace;letter-spacing:.14em;text-transform:uppercase;color:var(--muted);writing-mode:vertical-rl}.scroll-cue i{height:38px;width:1px;background:var(--line);position:relative;overflow:hidden}.scroll-cue i:after{content:"";position:absolute;top:-100%;height:55%;width:1px;background:var(--ink);animation:scrollCue 1.7s infinite var(--ease)}
.telemetry{position:absolute;right:clamp(20px,4vw,68px);top:50%;z-index:4;transform:translateY(-36%);width:138px}.telemetry-block{border-top:1px solid var(--line);padding:12px 0}.telemetry-block:last-child{border-bottom:1px solid var(--line)}.telemetry strong{font:600 9px "JetBrains Mono",monospace;letter-spacing:.13em;text-transform:uppercase;display:block}.telemetry span{display:block;font:10px/1.55 "JetBrains Mono",monospace;color:var(--muted);margin-top:5px}.telemetry .detail{opacity:0;transform:translateX(14px)}.progress{position:absolute;right:clamp(20px,4vw,68px);bottom:clamp(35px,8vh,82px);z-index:4;display:flex;gap:12px;align-items:end}.progress-number{font:500 10px "JetBrains Mono",monospace;color:var(--muted);width:22px}.progress-rail{height:94px;width:1px;background:var(--line);position:relative}.progress-fill{position:absolute;bottom:0;left:0;width:1px;height:0;background:var(--ink)}.progress-end{font:500 10px "JetBrains Mono",monospace;color:var(--muted)}.corner-index{position:absolute;top:94px;right:clamp(20px,4vw,68px);z-index:4;font:500 10px "JetBrains Mono",monospace;letter-spacing:.12em;color:var(--muted)}
.final-copy{position:absolute;left:clamp(20px,7vw,118px);top:clamp(120px,19vh,190px);width:265px;opacity:0;transform:translateY(16px);z-index:3}.final-copy h2{font-size:28px;line-height:.92;letter-spacing:-.075em;margin:7px 0 10px}.final-copy p{font:11px/1.65 "JetBrains Mono",monospace;color:var(--muted);margin:0}
.product-story{position:relative;z-index:2;overflow:hidden;background:#f4f4ee;padding:clamp(84px,10vw,150px) clamp(22px,6.5vw,104px) clamp(100px,12vw,180px);border-top:1px solid var(--line)}.product-story:before{content:"";position:absolute;inset:0;pointer-events:none;background:linear-gradient(90deg,transparent 24.95%,rgba(9,9,9,.055) 25%,transparent 25.08%,transparent 74.95%,rgba(9,9,9,.055) 75%,transparent 75.08%),radial-gradient(circle at 78% 10%,rgba(255,255,255,.96),transparent 30%)}.story-header,.story-layout{position:relative;z-index:1;max-width:1500px;margin-inline:auto}.story-header{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(280px,.65fr);gap:clamp(34px,6vw,100px);align-items:end;margin-bottom:clamp(62px,8vw,112px)}.story-kicker{display:flex;align-items:center;gap:10px;margin-bottom:22px;font:500 10px "JetBrains Mono",monospace;letter-spacing:.18em;text-transform:uppercase;color:var(--muted)}.story-kicker:before{content:"";width:8px;height:8px;border:1px solid var(--ink);border-radius:50%}.story-title{margin:0;font-family:"Inter Tight",Inter,sans-serif;font-size:clamp(50px,6.2vw,102px);font-weight:600;line-height:.91;letter-spacing:-.068em}.story-title span{display:block;color:#a6a6a0;font-weight:500}.story-intro{max-width:480px;margin:0 0 25px;font-size:clamp(15px,1.2vw,18px);line-height:1.65;color:#62625d}.story-cta{position:relative;display:inline-flex;align-items:center;gap:14px;min-height:48px;padding:0 20px;border:1px solid rgba(9,9,9,.22);border-radius:999px;color:var(--ink);background:rgba(255,255,255,.56);font:600 10px "JetBrains Mono",monospace;letter-spacing:.1em;text-transform:uppercase;text-decoration:none;overflow:hidden;isolation:isolate;transition:color .4s var(--ease),transform .4s var(--ease),box-shadow .4s var(--ease)}.story-cta:before{content:"";position:absolute;inset:0;background:var(--ink);border-radius:inherit;transform:translateX(-102%);transition:transform .5s var(--ease);z-index:-1}.story-cta svg{transition:transform .4s var(--ease)}.story-cta:hover{color:white;transform:translateY(-3px);box-shadow:0 16px 36px rgba(9,9,9,.13)}.story-cta:hover:before{transform:none}.story-cta:hover svg{transform:translateX(5px)}.story-layout{display:grid;grid-template-columns:minmax(0,1.42fr) minmax(340px,.78fr);gap:clamp(42px,7vw,112px);align-items:stretch}.story-media{position:relative;min-height:clamp(480px,58vw,760px);border-radius:22px;overflow:hidden;background:#151515;box-shadow:0 28px 70px rgba(9,9,9,.12)}.story-media img{width:100%;height:100%;object-fit:cover;display:block;transition:transform 1.1s var(--ease),filter 1.1s var(--ease)}.story-media:hover img{transform:scale(1.035);filter:contrast(1.04)}.story-media:after{content:"";position:absolute;inset:-50% auto -50% -45%;width:30%;pointer-events:none;background:linear-gradient(90deg,transparent,rgba(255,255,255,.17),transparent);transform:skewX(-14deg);animation:storyScan 7s ease-in-out infinite}.media-label{position:absolute;z-index:2;left:22px;top:22px;padding:9px 12px;border:1px solid rgba(255,255,255,.2);border-radius:999px;color:#ddd;background:rgba(0,0,0,.32);backdrop-filter:blur(10px);font:500 9px "JetBrains Mono",monospace;letter-spacing:.14em;text-transform:uppercase}.media-index{position:absolute;z-index:2;right:22px;bottom:22px;color:#fff;font:500 10px "JetBrains Mono",monospace;letter-spacing:.15em}.story-features{display:flex;flex-direction:column;justify-content:center}.story-reveal{opacity:0;transform:translateY(38px);filter:blur(8px);transition:opacity .9s var(--ease),transform .9s var(--ease),filter .9s var(--ease);transition-delay:var(--reveal-delay,0ms)}.story-reveal.is-visible{opacity:1;transform:none;filter:none}@keyframes storyScan{0%,35%{left:-45%;opacity:0}48%{opacity:1}70%,100%{left:125%;opacity:0}}@keyframes scrollCue{to{top:140%}}@media(max-width:980px){.story-header{grid-template-columns:1fr;align-items:start}.story-layout{grid-template-columns:1fr}.story-media{min-height:62vw}.story-features{display:grid;grid-template-columns:repeat(3,1fr)}}@media(max-width:850px){.nav{display:none}.video-wrap:before{background:linear-gradient(180deg,rgba(244,244,238,.24),transparent 28%,rgba(244,244,238,.72) 74%,var(--paper))}.product-video{object-position:60% center}.content{top:38vh;bottom:auto;width:calc(100% - 40px)}.title{font-size:clamp(60px,16vw,100px)}.description{max-width:430px}.telemetry{right:20px;top:94px;transform:none;width:112px}.telemetry-block:nth-child(2),.telemetry-block:nth-child(3){display:none}.corner-index{display:none}.final-copy{display:none}.progress{right:20px;bottom:30px}.scroll-cue{display:none}}@media(max-width:680px){.product-story{padding-inline:20px}.story-title{font-size:13.5vw}.story-media{min-height:78vw;border-radius:14px}.story-features{grid-template-columns:1fr}}@media(max-width:520px){.topbar{height:63px;padding:0 18px}.mode{font-size:8px;padding:7px 8px}.brand{font-size:16px}.content{left:20px;top:42vh;bottom:auto}.title{font-size:13.5vw}.description{font-size:13px;margin:19px 0}.button{flex:1;padding:0 12px}.telemetry{top:78px;right:18px}.progress{right:18px}.progress-rail{height:62px}.outline{-webkit-text-stroke-width:1px}}@media(prefers-reduced-motion:reduce){*,*:before,*:after{animation-duration:.01ms!important;animation-iteration-count:1!important;scroll-behavior:auto!important}.experience{height:130vh}.product-video{transform:none!important}.story-reveal{opacity:1;transform:none;filter:none}}
.story-media{margin:0}
.capabilities{position:relative;z-index:2;overflow:hidden;background:#11120f;color:#f5f5ef;padding:clamp(100px,11vw,172px) clamp(22px,6.5vw,104px) clamp(110px,12vw,190px);isolation:isolate}.capabilities:before{content:"";position:absolute;inset:0;z-index:-2;background:radial-gradient(circle at 18% 12%,rgba(223,255,0,.09),transparent 25%),radial-gradient(circle at 82% 70%,rgba(255,255,255,.07),transparent 30%),linear-gradient(rgba(255,255,255,.028) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.028) 1px,transparent 1px);background-size:auto,auto,64px 64px,64px 64px}.capabilities:after{content:"";position:absolute;z-index:-1;width:55vw;height:55vw;left:58%;top:38%;border:1px solid rgba(255,255,255,.06);border-radius:50%;box-shadow:0 0 0 9vw rgba(255,255,255,.012),0 0 0 18vw rgba(255,255,255,.008);animation:capOrbit 18s linear infinite}.cap-inner{position:relative;max-width:1500px;margin:auto}.cap-heading{text-align:center;max-width:1080px;margin:0 auto clamp(68px,8vw,116px)}.cap-kicker{display:inline-flex;align-items:center;gap:12px;color:#a7a89f;font:500 10px "JetBrains Mono",monospace;letter-spacing:.2em;text-transform:uppercase}.cap-kicker:before,.cap-kicker:after{content:"";width:28px;height:1px;background:#53544e}.cap-title{margin:26px 0 22px;font-family:"Inter Tight",Inter,sans-serif;font-size:clamp(52px,6.7vw,108px);font-weight:600;line-height:.92;letter-spacing:-.068em}.cap-title span{display:block;color:transparent;-webkit-text-stroke:1px rgba(245,245,239,.46)}.cap-lead{max-width:590px;margin:0 auto;color:#9b9c94;font-size:clamp(14px,1.15vw,17px);line-height:1.7}.cap-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:clamp(14px,1.8vw,28px);perspective:1200px}.cap-card{--mx:50%;--my:50%;--rx:0deg;--ry:0deg;position:relative;min-height:clamp(430px,36vw,550px);padding:clamp(28px,3vw,46px);display:flex;flex-direction:column;overflow:hidden;border:1px solid rgba(255,255,255,.12);border-radius:3px;background:linear-gradient(145deg,rgba(255,255,255,.09),rgba(255,255,255,.025));box-shadow:0 18px 50px rgba(0,0,0,.18);transform:rotateX(var(--rx)) rotateY(var(--ry));transform-style:preserve-3d;transition:transform .18s ease,border-color .5s var(--ease),box-shadow .5s var(--ease),background .5s var(--ease)}.cap-card:nth-child(2){transform:translateY(28px) rotateX(var(--rx)) rotateY(var(--ry))}.cap-card:before{content:"";position:absolute;inset:0;pointer-events:none;opacity:0;background:radial-gradient(420px circle at var(--mx) var(--my),rgba(255,255,255,.17),transparent 44%);transition:opacity .45s ease}.cap-card:after{content:"";position:absolute;left:0;bottom:0;width:100%;height:3px;background:linear-gradient(90deg,var(--signal),#fff);transform:scaleX(.12);transform-origin:left;transition:transform .75s var(--ease)}.cap-card:hover{border-color:rgba(255,255,255,.35);background:linear-gradient(145deg,rgba(255,255,255,.13),rgba(255,255,255,.04));box-shadow:0 34px 90px rgba(0,0,0,.4),0 0 0 1px rgba(223,255,0,.08)}.cap-card:hover:before{opacity:1}.cap-card:hover:after{transform:scaleX(1)}.cap-card-top{position:relative;z-index:2;display:flex;align-items:flex-start;justify-content:space-between;transform:translateZ(28px)}.cap-icon{width:52px;height:52px;display:grid;place-items:center;border:1px solid rgba(255,255,255,.16);background:rgba(255,255,255,.075);transition:color .45s var(--ease),background .45s var(--ease),transform .55s var(--ease),border-radius .55s var(--ease)}.cap-card:hover .cap-icon{color:#11120f;background:var(--signal);transform:rotate(-7deg) scale(1.08);border-radius:50%}.cap-index{font:500 10px "JetBrains Mono",monospace;letter-spacing:.15em;color:#777970}.cap-ghost{position:absolute;right:-18px;top:40px;color:transparent;-webkit-text-stroke:1px rgba(255,255,255,.055);font-family:"Inter Tight",Inter,sans-serif;font-size:clamp(150px,17vw,260px);font-weight:700;line-height:1;letter-spacing:-.09em;transition:transform .8s var(--ease),-webkit-text-stroke-color .5s ease}.cap-card:hover .cap-ghost{transform:translate(-12px,10px) rotate(-4deg);-webkit-text-stroke-color:rgba(223,255,0,.15)}.cap-card-copy{position:relative;z-index:2;margin-top:auto;transform:translateZ(34px);transition:transform .55s var(--ease)}.cap-card:hover .cap-card-copy{transform:translate3d(0,-9px,34px)}.cap-card h3{margin:0 0 15px;font-family:"Inter Tight",Inter,sans-serif;font-size:clamp(29px,2.6vw,42px);line-height:.98;font-weight:600;letter-spacing:-.045em}.cap-card p{max-width:360px;margin:0;color:#999b93;font-size:14px;line-height:1.7;transition:color .45s ease}.cap-card:hover p{color:#d4d5ce}.cap-card-foot{position:relative;z-index:2;display:flex;justify-content:space-between;align-items:center;margin-top:34px;padding-top:18px;border-top:1px solid rgba(255,255,255,.12);font:500 9px "JetBrains Mono",monospace;letter-spacing:.14em;text-transform:uppercase;color:#83857d;transform:translateZ(30px)}.cap-card-foot svg{transition:transform .45s var(--ease),color .45s ease}.cap-card:hover .cap-card-foot svg{transform:translate(4px,-4px);color:var(--signal)}.cap-footer{display:flex;align-items:center;justify-content:space-between;gap:28px;margin-top:clamp(76px,9vw,130px);padding-top:22px;border-top:1px solid rgba(255,255,255,.13)}.cap-note{color:#74766e;font:500 9px/1.6 "JetBrains Mono",monospace;letter-spacing:.13em;text-transform:uppercase}.cap-cta{position:relative;min-height:50px;padding:0 21px;display:inline-…3309 tokens truncated…0;width:100%;height:2px;pointer-events:none;transform:scaleX(0);transform-origin:left;background:linear-gradient(90deg,#11120f 0%,#6d6e68 72%,rgba(109,110,104,0) 100%)}.page-progress:after{content:"";position:absolute;right:-1px;top:-2px;width:6px;height:6px;border-radius:50%;background:#11120f;box-shadow:0 0 12px rgba(17,18,15,.3)}
.product-story,.capabilities,.finale{box-shadow:inset 0 1px rgba(255,255,255,.48),0 -24px 65px rgba(17,18,15,.025)}.product-story:after{content:"";position:absolute;z-index:0;width:52vw;height:52vw;right:-18vw;top:2%;pointer-events:none;border-radius:50%;border:1px solid rgba(17,18,15,.045);box-shadow:0 0 0 7vw rgba(17,18,15,.008),0 0 0 14vw rgba(17,18,15,.006);animation:ambientFloat 16s ease-in-out infinite alternate}.product-story>*{position:relative;z-index:1}
.story-kicker:before,.cap-kicker:before,.finale-meta:before{animation:softPulse 2.8s ease-in-out infinite}.story-title span{background:linear-gradient(90deg,#9c9d97 0%,#686963 48%,#9c9d97 100%);background-size:220% 100%;background-clip:text;-webkit-background-clip:text;color:transparent;animation:silverType 9s ease-in-out infinite}.story-media{outline:1px solid rgba(255,255,255,.48);outline-offset:-1px}.story-media:before{content:"";position:absolute;z-index:2;inset:12px;pointer-events:none;border:1px solid rgba(255,255,255,.13);border-radius:13px;transition:inset .8s var(--ease),border-color .8s ease}.story-media:hover:before{inset:18px;border-color:rgba(255,255,255,.25)}
.story-reveal{transform:translateY(38px) scale(.985);transition-duration:1s}.story-reveal.is-visible{transform:translateY(0) scale(1)}.capabilities:before{animation:gridDrift 24s linear infinite}.cap-card{will-change:transform}.cap-card h3:after{content:"";display:block;width:20px;height:1px;margin-top:15px;background:#b7b8b1;transition:width .65s var(--ease),background .45s ease}.cap-card:hover h3:after{width:54px;background:#33342f}
.button,.story-cta,.cap-cta,.finale-cta{-webkit-tap-highlight-color:transparent}.button:active,.story-cta:active,.cap-cta:active,.finale-cta:active{transform:translateY(0) scale(.975)}.button:after,.story-cta:after,.cap-cta:after,.finale-cta:after{content:"";position:absolute;z-index:2;top:-120%;left:-35%;width:26%;height:340%;pointer-events:none;background:linear-gradient(90deg,transparent,rgba(255,255,255,.42),transparent);transform:rotate(18deg) translateX(-300%);transition:transform .8s var(--ease)}.button:hover:after,.story-cta:hover:after,.cap-cta:hover:after,.finale-cta:hover:after{transform:rotate(18deg) translateX(620%)}button:focus-visible,a:focus-visible{outline-color:#898a84}
.finale-card{animation:cardBreathe 7s ease-in-out infinite}.finale-card:hover{animation-play-state:paused}.finale-grain{opacity:.3}.finale-frame:before,.finale-frame:after{content:"";position:absolute;width:42px;height:42px;border-color:rgba(255,255,255,.52)}.finale-frame:before{left:-1px;top:-1px;border-left:2px solid;border-top:2px solid}.finale-frame:after{right:-1px;bottom:-1px;border-right:2px solid;border-bottom:2px solid}
@keyframes ambientFloat{from{transform:translate3d(0,-2%,0) rotate(0deg)}to{transform:translate3d(-4%,5%,0) rotate(12deg)}}@keyframes softPulse{0%,100%{opacity:.45;transform:scale(.82)}50%{opacity:1;transform:scale(1)}}@keyframes silverType{0%,100%{background-position:100% 50%}50%{background-position:0 50%}}@keyframes gridDrift{to{background-position:0 0,0 0,64px 64px,64px 64px}}@keyframes cardBreathe{0%,100%{box-shadow:0 30px 90px rgba(0,0,0,.38),inset 0 1px 0 rgba(255,255,255,.05)}50%{box-shadow:0 34px 105px rgba(0,0,0,.43),inset 0 1px 0 rgba(255,255,255,.07)}}
@supports(animation-timeline:scroll()){.page-progress{animation:siteProgress linear both;animation-timeline:scroll(root block)}@keyframes siteProgress{to{transform:scaleX(1)}}.product-story:after{animation:ambientFloat linear both;animation-timeline:view();animation-range:entry 0% exit 100%}}
@media(max-width:680px){body:before{opacity:.026}.product-story:after{width:90vw;height:90vw}.page-progress{height:1px}}
@media(prefers-reduced-motion:reduce){.product-story:after,.story-title span,.story-kicker:before,.cap-kicker:before,.finale-meta:before,.capabilities:before,.finale-card{animation:none!important}.story-reveal{transition-duration:.01ms}}

/* Repair: light editorial treatment for the third fold */
.capabilities{
  --cap-ink:#11120f;
  --cap-muted:#71726d;
  --cap-line:rgba(17,18,15,.13);
  background:#f7f7f3;
  color:var(--cap-ink);
  border-top:1px solid var(--line)
}
.capabilities:before{
  background:
    radial-gradient(circle at 13% 18%,rgba(255,255,255,.96),transparent 27%),
    radial-gradient(circle at 84% 72%,rgba(17,18,15,.035),transparent 30%),
    linear-gradient(rgba(17,18,15,.035) 1px,transparent 1px),
    linear-gradient(90deg,rgba(17,18,15,.035) 1px,transparent 1px);
  background-size:auto,auto,64px 64px,64px 64px
}
.capabilities:after{
  border-color:rgba(17,18,15,.055);
  box-shadow:0 0 0 9vw rgba(17,18,15,.012),0 0 0 18vw rgba(17,18,15,.008)
}
.cap-kicker{color:#797a74}
.cap-kicker:before,.cap-kicker:after{background:#b7b8b1}
.cap-title span{color:#858680;-webkit-text-stroke:0;font-weight:500}
.cap-lead{color:#686963}
.cap-card{
  border-color:rgba(17,18,15,.12);
  background:linear-gradient(145deg,rgba(255,255,255,.96),rgba(246,246,241,.76));
  box-shadow:0 18px 48px rgba(30,31,27,.065)
}
.cap-card:before{
  background:
    radial-gradient(380px circle at var(--mx) var(--my),rgba(17,18,15,.075),transparent 39%),
    radial-gradient(260px circle at calc(var(--mx) + 70px) calc(var(--my) - 70px),rgba(255,255,255,.72),transparent 45%)
}
.cap-card:after{background:linear-gradient(90deg,#1a1b18 0%,#8e8f89 48%,#d9d9d2 100%)}
.cap-card:hover{
  border-color:rgba(17,18,15,.26);
  background:linear-gradient(145deg,#fff,#f1f1ec);
  box-shadow:0 32px 74px rgba(28,29,26,.13),0 0 0 1px rgba(17,18,15,.035)
}
.cap-icon{border-color:rgba(17,18,15,.14);background:rgba(17,18,15,.035)}
.cap-card:hover .cap-icon{color:#fff;background:#171815;box-shadow:0 10px 26px rgba(17,18,15,.16)}
.cap-index{color:#8d8e88}
.cap-ghost{-webkit-text-stroke-color:rgba(17,18,15,.06)}
.cap-card:hover .cap-ghost{-webkit-text-stroke-color:rgba(17,18,15,.14)}
.cap-card p{color:#777872}
.cap-card:hover p{color:#555650}
.cap-card-foot{border-top-color:rgba(17,18,15,.11);color:#858680}
.cap-card:hover .cap-card-foot svg{color:#171815}
.cap-footer{border-top-color:rgba(17,18,15,.13)}
.cap-note{color:#81827c}
.cap-cta{border-color:rgba(17,18,15,.22);color:var(--cap-ink);background:rgba(255,255,255,.68)}
.cap-cta:before{background:#171815}
.cap-cta:hover{color:#fff;border-color:#171815;box-shadow:0 14px 32px rgba(17,18,15,.14)}

/* Repair: complete closing fold */
.finale{
  position:relative;
  z-index:2;
  min-height:max(760px,100svh);
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:clamp(28px,4vw,48px);
  padding:clamp(82px,8vw,118px) 0 clamp(42px,5vw,64px);
  overflow:hidden;
  background:#d7d6d0;
  color:#f5f5ef;
  isolation:isolate
}
.finale-bg{
  position:absolute;
  inset:-4%;
  z-index:-4;
  width:108%;
  height:108%;
  object-fit:cover;
  object-position:center;
  filter:grayscale(.18) saturate(.58) contrast(.92) brightness(.72);
  transform:scale(1.025);
  will-change:transform
}
.finale-shade{
  position:absolute;
  inset:0;
  z-index:-3;
  background:
    radial-gradient(circle at 50% 48%,rgba(20,20,18,.08),rgba(20,20,18,.38) 62%,rgba(20,20,18,.55) 100%),
    linear-gradient(180deg,rgba(17,18,15,.08),rgba(17,18,15,.32))
}
.finale-grain{
  position:absolute;
  inset:0;
  z-index:-2;
  pointer-events:none;
  opacity:.22;
  mix-blend-mode:soft-light;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.36'/%3E%3C/svg%3E")
}
.finale-frame{
  position:absolute;
  inset:clamp(22px,3.2vw,52px);
  z-index:-1;
  border:1px solid rgba(255,255,255,.28);
  pointer-events:none
}
.finale-meta{
  position:absolute;
  top:clamp(42px,5vw,78px);
  right:clamp(42px,6vw,98px);
  display:flex;
  align-items:center;
  gap:11px;
  color:rgba(255,255,255,.72);
  font-family:var(--mono);
  font-size:9px;
  font-weight:600;
  letter-spacing:.24em;
  text-transform:uppercase
}
.finale-meta:before{content:"";width:5px;height:5px;border-radius:50%;background:#fff}
.finale-orbit{
  position:absolute;
  left:50%;
  top:50%;
  z-index:-1;
  width:min(68vw,880px);
  aspect-ratio:1;
  border:1px solid rgba(255,255,255,.18);
  border-radius:50%;
  transform:translate(-50%,-50%);
  pointer-events:none
}
.finale-orbit:before,.finale-orbit:after{
  content:"";
  position:absolute;
  border:1px solid rgba(255,255,255,.07);
  border-radius:50%
}
.finale-orbit:before{inset:12%}
.finale-orbit:after{inset:28%}
.finale-card{
  --shine-x:50%;
  position:relative;
  width:min(calc(100vw - 52px),390px);
  padding:clamp(46px,5vw,66px) clamp(34px,4vw,54px) clamp(38px,4.2vw,52px);
  display:flex;
  flex-direction:column;
  align-items:center;
  text-align:center;
  overflow:hidden;
  border:1px solid rgba(255,255,255,.15);
  border-radius:14px;
  background:linear-gradient(145deg,rgba(18,18,17,.96),rgba(9,9,9,.94));
  box-shadow:0 30px 90px rgba(0,0,0,.38),inset 0 1px 0 rgba(255,255,255,.05);
  backdrop-filter:blur(14px);
  -webkit-backdrop-filter:blur(14px)
}
.finale-card:before{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  opacity:.5;
  background:radial-gradient(380px circle at var(--shine-x) 0%,rgba(255,255,255,.13),transparent 43%)
}
.finale-card>*{position:relative;z-index:1}
.finale-mark{
  width:58px;
  height:58px;
  margin-bottom:29px;
  display:grid;
  place-items:center;
  border-radius:50%;
  background:#f7f7f3;
  color:#11120f;
  font-size:19px;
  font-weight:760;
  box-shadow:0 0 0 9px rgba(255,255,255,.045),0 9px 30px rgba(0,0,0,.28)
}
.finale-overline{
  margin-bottom:18px;
  color:rgba(255,255,255,.52);
  font-family:var(--mono);
  font-size:8px;
  font-weight:600;
  letter-spacing:.3em;
  text-transform:uppercase
}
.finale-title{
  margin:0;
  color:#fff;
  font-size:clamp(36px,3.5vw,49px);
  font-weight:620;
  line-height:.95;
  letter-spacing:-.065em
}
.finale-title span{color:#a7a8a2;font-weight:350}
.finale-copy{
  max-width:280px;
  margin:23px auto 29px;
  color:rgba(255,255,255,.58);
  font-size:13px;
  line-height:1.65
}
.finale-cta{
  position:relative;
  z-index:0;
  isolation:isolate;
  min-height:48px;
  padding:0 22px;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:12px;
  overflow:hidden;
  border:1px solid rgba(255,255,255,.92);
  border-radius:999px;
  background:#f7f7f3;
  color:#11120f;
  font-family:var(--mono);
  font-size:9px;
  font-weight:700;
  letter-spacing:.1em;
  text-decoration:none;
  text-transform:uppercase;
  transition:transform .35s var(--ease),background .35s ease,color .35s ease,box-shadow .35s ease
}
.finale-cta:before{content:"";position:absolute;z-index:-1;inset:0;background:#242520;transform:translateY(102%);transition:transform .45s var(--ease)}
.finale-cta>*{position:relative;z-index:1}
.finale-cta:hover{color:#fff;transform:translateY(-2px);box-shadow:0 14px 30px rgba(0,0,0,.26)}
.finale-cta:hover:before{transform:translateY(0)}
.finale-cta svg{transition:transform .35s var(--ease)}
.finale-cta:hover svg{transform:translateY(-3px)}
.finale-signature{
  width:100%;
  margin-top:31px;
  padding-top:21px;
  border-top:1px solid rgba(255,255,255,.12);
  color:rgba(255,255,255,.38);
  font-family:var(--mono);
  font-size:8px;
  letter-spacing:.25em;
  text-transform:uppercase
}

@media(max-width:980px){
  .finale{min-height:820px;padding:96px 24px 54px}
  .finale-orbit{width:92vw}
}
@media(max-width:680px){
  .finale{min-height:760px;padding:82px 18px 42px}
  .finale-frame{inset:14px}
  .finale-meta{top:29px;right:30px}
  .finale-card{width:min(calc(100vw - 52px),360px);padding:44px 28px 38px}
}
@media(prefers-reduced-motion:reduce){.finale-bg{will-change:auto}.finale-card{animation:none!important}}

/* Mobile: preserva a proporção e a leitura dos grupos de três cards. */
@media(max-width:900px){
  .story-features{grid-template-columns:1fr}
  .capabilities{padding-inline:20px}
  .cap-grid{grid-template-columns:1fr;gap:16px;perspective:none}
  .cap-card,.cap-card:nth-child(2){min-height:340px;padding:30px 24px;transform:none}
  .cap-card-top,.cap-card-copy,.cap-card-foot{transform:none}
  .cap-card:hover .cap-card-copy{transform:translateY(-6px)}
  .cap-ghost{font-size:150px}
  .cap-footer{flex-direction:column;align-items:flex-start}
}
@media(max-width:520px){
  .capabilities{padding-block:84px 100px}
  .cap-heading{margin-bottom:48px}
  .cap-card,.cap-card:nth-child(2){min-height:300px;padding:26px 22px}
  .cap-card h3{font-size:30px}
}

  </style>
  <div class="page-progress" aria-hidden="true"></div>
  <a class="skip" href="#hero-copy">Pular para o conteúdo</a>
  <main>
    <section class="experience" aria-label="Apresentação da caneta especial">
      <div class="stage" id="stage">
        <div class="video-wrap" id="movie">
          <video id="pen-video" class="product-video" muted playsinline autoplay loop preload="auto" aria-label="Vídeo de apresentação da caneta"><source src="/assets/media/video2-mobile.mp4" type="video/mp4"></video>
        </div>
        <div class="content" id="hero-copy">
          <div class="eyebrow">{hero.eyebrow}</div>
          <h1 class="title" aria-label="A caneta mais avançada do mundo"><span class="title-line"><span>{hero.titleLine1}</span></span><span class="title-line"><span class="outline">{hero.titleLine2}</span></span><span class="title-line"><span>{hero.titleLine3}</span></span></h1>
          <p class="description">{hero.description}</p>
          <div class="actions"><Button href="#design" variant="primary">{hero.primaryButton}</Button><Button href="#movie" variant="secondary" icon="right">{hero.secondaryButton}</Button></div>
        </div>
        <div class="final-copy"><span class="token">No seu ritmo</span><h2>O gesto vira linguagem.</h2><p>Uma caneta especial, vista de perto e conduzida por você.</p></div>
        <div class="scroll-cue" aria-hidden="true"><span>Role para explorar</span><i></i></div>
        <div class="progress" aria-label="Progresso da apresentação"><span class="progress-number" id="progress-number">00</span><span class="progress-rail"><i class="progress-fill"></i></span><span class="progress-end">100</span></div>
      </div>
    </section>
    <section class="product-story" id="design" aria-labelledby="story-title">
      <div class="story-header">
        <div class="story-reveal">
          <div class="story-kicker">Desenho do produto</div>
          <h2 class="story-title" id="story-title">Forma precisa.<span>Presença essencial.</span></h2>
        </div>
        <div class="story-reveal" style="--reveal-delay:140ms">
          <p class="story-intro">Pensada para acompanhar o fluxo das ideias, a Artools transforma simplicidade visual em uma experiência direta, expressiva e confortável.</p>
          <Button href="#story-features" variant="story" icon="right">Conhecer o desenho</Button>
        </div>
      </div>
      <div class="story-layout">
        <figure class="story-media story-reveal" style="--reveal-delay:100ms">
          <Image src={heroPoster} alt="Caneta Artools branca fotografada em estúdio sobre fundo grafite" width={1200} format="webp" quality={80} loading="lazy" />
        </figure>
        <div class="story-features" id="story-features">
          <StoryFeatureCard
            title="Controle natural"
            description="Uma forma alongada e limpa que aproxima mão, gesto e página, deixando a atenção permanecer onde importa: na ideia."
            delay={160}
          />
          <StoryFeatureCard
            title="Identidade essencial"
            description="O corpo branco, os contrastes escuros e os detalhes contidos criam uma presença reconhecível sem excesso visual."
            delay={240}
          />
          <StoryFeatureCard
            title="Liberdade criativa"
            description="Da anotação rápida ao traço mais cuidadoso, um instrumento concebido para acompanhar diferentes maneiras de pensar e criar."
            delay={320}
          />
        </div>
      </div>
    </section>
    <section class="capabilities" id="technology" aria-labelledby="cap-title">
      <div class="cap-inner">
        <header class="cap-heading story-reveal">
          <div class="cap-kicker">Experiência de escrita</div>
          <h2 class="cap-title" id="cap-title">Quase desaparece na mão.<span>Permanece em cada ideia.</span></h2>
          <p class="cap-lead">Cada detalhe foi reduzido ao essencial para que nada interrompa o pensamento entre a primeira intenção e o último traço.</p>
        </header>
        <div class="cap-grid">
          <article class="cap-card story-reveal" style="--reveal-delay:80ms">
            <div class="cap-card-top">
              <span class="cap-icon" aria-hidden="true"><svg width="23" height="23" viewBox="0 0 24 24" fill="none"><path d="M4 16.5V20h3.5L19 8.5 15.5 5 4 16.5Z" stroke="currentColor" stroke-width="1.4"/><path d="m13.8 6.7 3.5 3.5M4 13h4" stroke="currentColor" stroke-width="1.4"/></svg></span>
              <span class="cap-index" aria-hidden="true"></span>
            </div>
            <span class="cap-ghost" aria-hidden="true"></span>
            <div class="cap-card-copy"><h3>Traço sem intervalo</h3><p>A resposta acompanha o movimento com naturalidade, criando uma escrita contínua que deixa a ideia seguir antes que ela se perca.</p></div>
            <div class="cap-card-foot"><span>Fluxo intuitivo</span><svg width="15" height="15" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M3 13 13 3M6 3h7v7" stroke="currentColor" stroke-width="1.2"/></svg></div>
          </article>
          <article class="cap-card story-reveal" style="--reveal-delay:170ms">
            <div class="cap-card-top">
              <span class="cap-icon" aria-hidden="true"><svg width="23" height="23" viewBox="0 0 24 24" fill="none"><path d="M5 9v6M9 6v12M13 9v6M17 7v10M21 10v4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/></svg></span>
              <span class="cap-index" aria-hidden="true"></span>
            </div>
            <span class="cap-ghost" aria-hidden="true"></span>
            <div class="cap-card-copy"><h3>Silêncio que concentra</h3><p>Uma presença discreta, pensada para ambientes onde a atenção precisa permanecer inteira e o gesto deve falar mais alto.</p></div>
            <div class="cap-card-foot"><span>Presença mínima</span><svg width="15" height="15" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M3 13 13 3M6 3h7v7" stroke="currentColor" stroke-width="1.2"/></svg></div>
          </article>
          <article class="cap-card story-reveal" style="--reveal-delay:260ms">
            <div class="cap-card-top">
              <span class="cap-icon" aria-hidden="true"><svg width="23" height="23" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="8" stroke="currentColor" stroke-width="1.4"/><path d="M12 4v16M4 12h16M8.5 8.5l7 7" stroke="currentColor" stroke-width="1.1"/></svg></span>
              <span class="cap-index" aria-hidden="true"></span>
            </div>
            <span class="cap-ghost" aria-hidden="true"></span>
            <div class="cap-card-copy"><h3>Equilíbrio em movimento</h3><p>Proporção, leveza visual e apoio confortável se encontram para acompanhar anotações rápidas ou longas sessões de criação.</p></div>
            <div class="cap-card-foot"><span>Conforto contínuo</span><svg width="15" height="15" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M3 13 13 3M6 3h7v7" stroke="currentColor" stroke-width="1.2"/></svg></div>
          </article>
        </div>
        <footer class="cap-footer story-reveal" style="--reveal-delay:180ms">
          <span class="cap-note">Artools Precision Pen<br>Design para acompanhar ideias.</span>
          <Button href="#design" variant="capability" icon="right">Rever o desenho</Button>
        </footer>
      </div>
    </section>
    <section class="finale" id="finale" aria-labelledby="finale-title">
      <Image class="finale-bg" src={closingFrame} alt="Caneta Artools branca suspensa em uma composição de estúdio" width={1600} format="webp" quality={82} loading="lazy" />
      <div class="finale-shade" aria-hidden="true"></div>
      <div class="finale-grain" aria-hidden="true"></div>
      <div class="finale-frame" aria-hidden="true"></div>
      <div class="finale-meta">O gesto final</div>
      <div class="finale-orbit" aria-hidden="true"></div>
      <PenMarquee />
      <div class="finale-card story-reveal">
        <div class="finale-mark" aria-hidden="true">O</div>
        <span class="finale-overline">Instrumento de criação</span>
        <h2 class="finale-title" id="finale-title">Artools<span>.Precision</span></h2>
        <p class="finale-copy">Precisão que acompanha o seu ritmo — da primeira intenção ao traço que permanece.</p>
        <Button href="#hero-copy" variant="finale" icon="up">Recomeçar a experiência</Button>
        <div class="finale-signature"><span>Edição especial</span></div>
      </div>
    </section>
    <TechnicalSpecs />
  </main>
  <script is:inline>
const video=document.querySelector('#pen-video'), stage=document.querySelector('#stage'), number=document.querySelector('#progress-number'), fill=document.querySelector('.progress-fill'), reduced=matchMedia('(prefers-reduced-motion: reduce)').matches, mobilePlayback=matchMedia('(max-width: 850px), (pointer: coarse)').matches;
function updateProgress(progress){number.textContent=String(Math.round(progress*100)).padStart(2,'0');fill.style.height=(progress*100)+'%'}
let videoDuration=0, requestedTime=0, renderedTime=-1, seekFrame=0;
const videoFrame=1/24;
function driveVideo(){
  seekFrame=0;
  if(mobilePlayback||!videoDuration||video.seeking)return;
  const delta=requestedTime-video.currentTime;
  if(Math.abs(delta)<videoFrame*.55)return;
  const step=Math.sign(delta)*Math.min(Math.abs(delta)*.32,.24);
  const nextFrame=Math.min(videoDuration-.04,Math.max(0,Math.round((video.currentTime+step)/videoFrame)*videoFrame));
  if(Math.abs(nextFrame-renderedTime)<videoFrame*.55)return;
  try{renderedTime=nextFrame;video.currentTime=nextFrame}catch(e){}
}
function requestVideoDrive(){if(!seekFrame)seekFrame=requestAnimationFrame(driveVideo)}
function startMobileVideo(){
  if(!mobilePlayback||reduced)return;
  video.muted=true;
  video.loop=true;
  const playback=video.play();
  if(playback)playback.catch(()=>{});
}
video.addEventListener('loadedmetadata',()=>{document.documentElement.classList.add('is-ready');videoDuration=video.duration;
  if(mobilePlayback&&!reduced)startMobileVideo();else{video.pause();video.currentTime=0;}
  if(!reduced)document.querySelector('.topbar').animate([{transform:'translateY(-18px)',opacity:0},{transform:'translateY(0)',opacity:1}],{duration:550,easing:'cubic-bezier(.16,1,.3,1)'})});
video.addEventListener('canplay',startMobileVideo,{once:true});
if(mobilePlayback&&!reduced){
  document.addEventListener('touchstart',startMobileVideo,{once:true,passive:true});
  document.addEventListener('pointerdown',startMobileVideo,{once:true,passive:true});
}
video.addEventListener('error',()=>document.documentElement.classList.add('video-unavailable'));
if(reduced){video.pause();}
const experience=document.querySelector('.experience');
const contentParts=[...document.querySelectorAll('.eyebrow,.title-line,.description,.actions')];
let parallaxFrame=0;
function renderParallax(){
  parallaxFrame=0;
  const travel=Math.max(1,experience.offsetHeight-innerHeight);
  const scrollOffset=Math.max(0,-experience.getBoundingClientRect().top);
  const progress=Math.min(1,Math.max(0,scrollOffset/travel));
  if(!mobilePlayback){
    requestedTime=Math.min(Math.max(0,videoDuration-.04),progress*videoDuration);
    requestVideoDrive();
  }
  video.style.transform=`scale(${1+progress*.025})`;
  const textProgress=Math.min(1,scrollOffset/(innerHeight*.58));
  contentParts.forEach((part,index)=>{
    const local=Math.min(1,Math.max(0,(textProgress-index*.055)/.68));
    const eased=1-Math.pow(1-local,3);
    part.style.transform=`translate3d(0,${-eased*(28+index*7)}px,0)`;
    part.style.opacity=String(1-eased);
  });
  updateProgress(progress);
}
function requestParallax(){if(!parallaxFrame)parallaxFrame=requestAnimationFrame(renderParallax)}
addEventListener('scroll',requestParallax,{passive:true});
addEventListener('resize',requestParallax,{passive:true});
if(!mobilePlayback)video.addEventListener('seeked',requestVideoDrive);
renderParallax();
const storyObserver=new IntersectionObserver(entries=>entries.forEach(entry=>entry.target.classList.toggle('is-visible',entry.isIntersecting)),{threshold:.12,rootMargin:'-3% 0px -3% 0px'});
document.querySelectorAll('.story-reveal').forEach(element=>storyObserver.observe(element));
if(!reduced&&matchMedia('(hover:hover) and (pointer:fine)').matches){
  document.querySelectorAll('.cap-card').forEach(card=>{
    let cardFrame=0,pointerX=0,pointerY=0;
    card.addEventListener('pointermove',event=>{
      const bounds=card.getBoundingClientRect();
      pointerX=event.clientX-bounds.left;pointerY=event.clientY-bounds.top;
      if(cardFrame)return;
      cardFrame=requestAnimationFrame(()=>{
        cardFrame=0;
        const x=pointerX/bounds.width,y=pointerY/bounds.height;
        card.style.setProperty('--mx',`${pointerX}px`);card.style.setProperty('--my',`${pointerY}px`);
        card.style.setProperty('--rx',`${(0.5-y)*5}deg`);card.style.setProperty('--ry',`${(x-0.5)*6}deg`);
      });
    },{passive:true});
    card.addEventListener('pointerleave',()=>{card.style.setProperty('--rx','0deg');card.style.setProperty('--ry','0deg')},{passive:true});
  });
  const finaleCard=document.querySelector('.finale-card');
  finaleCard.addEventListener('pointermove',event=>{const bounds=finaleCard.getBoundingClientRect();finaleCard.style.setProperty('--fx',`${event.clientX-bounds.left}px`);finaleCard.style.setProperty('--fy',`${event.clientY-bounds.top}px`)},{passive:true});
}
const finale=document.querySelector('.finale'),finaleImage=document.querySelector('.finale-bg');
let finaleFrame=0;
function renderFinale(){finaleFrame=0;if(reduced)return;const bounds=finale.getBoundingClientRect();if(bounds.bottom<0||bounds.top>innerHeight)return;const offset=Math.max(-26,Math.min(26,(innerHeight*.5-(bounds.top+bounds.height*.5))*.035));finaleImage.style.transform=`translate3d(0,${offset}px,0) scale(1.02)`}
function requestFinale(){if(!finaleFrame)finaleFrame=requestAnimationFrame(renderFinale)}
addEventListener('scroll',requestFinale,{passive:true});addEventListener('resize',requestFinale,{passive:true});renderFinale();


  </script>
</Layout>
